{
  "name": "text-overlay-generator",
  "version": "1.0.0",
  "description": "Generate text overlays for video production",
  "main": "api/generate-text.js",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^14.0.0"
  },
  "engines": {
    "node": "22.x"
  }
}
